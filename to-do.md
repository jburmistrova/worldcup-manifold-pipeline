# To-Do

Personal action items and learning follow-ups — not project scope (that's tracked in `PROJECT_SPEC.md`'s "Scope: core vs. stretch"). This is smaller-grained: things worth coming back to that don't need a whole doc section of their own.

- [ ] **Practice SQL window functions from first principles.** After building `int_market_implied_probability`, flagged that I couldn't have written that query cold in an interview. Plan: build up from the basics (`PARTITION BY`/`ORDER BY` anatomy → `ROW_NUMBER`/`RANK` → the running-sum default behavior → assembling a VWAP from two nested running sums) with hands-on practice, not just re-reading the explanation.
